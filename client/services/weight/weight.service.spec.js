'use strict';

describe('Service: weight', function () {

  // load the service's module
  beforeEach(angular.mock.module('prindleApp'));

  // instantiate service
  var weight;
  beforeEach(inject(function (_weight_) {
    weight = _weight_;
  }));

//  it('should do something', function () {
//    expect(!!weight).toBe(true);
//  });

});
