'use strict';

describe('Service: guiState', function () {

  // load the service's module
  beforeEach(angular.mock.module('prindleApp'));

  // instantiate service
  var guiState;
  beforeEach(inject(function (_guiState_) {
    guiState = _guiState_;
  }));

//  it('should do something', function () {
//    expect(!!guiState).toBe(true);
//  });

});
